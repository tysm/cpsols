#include <cpplib/stdinc.hpp>

int32_t main(){
    freopen("tests.in", "r", stdin);
    desync();
    int t;
    cin >> t;
    while(t--){
        int n, m;
        cin >> n >> m;
        memset(cnt, 0, sizeof(cnt));
    }
    return 0;
}
