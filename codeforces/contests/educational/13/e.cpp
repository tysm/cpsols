#include <cpplib/stdinc.hpp>

int32_t main(){
    desync();
    int n;
    cin >> n;
    vvd probs(n+1);
    for(int i=1; i<=n; ++i){
        for(int j=1; j<=n; ++j)
            cin >> probs[i][j];
    }
    double loss = 0;
    for(int i=1; i<=n; ++i){

    }
    cout << fixed << setprecision(15) << (double)1 - looses()
    return 0;
}
